// Esto nos sirve capturar errores en el código. Es decir se pude aplicar a una parte
// del código para arrojar un mensaje al usuario cuando se produzca un  error
// dentro del "try" se mete el código susceptible a fallo
try {
    let year = 2019;
    alert(yea);
} catch (error) { console.log('Ha ocurrido un error en el código') };